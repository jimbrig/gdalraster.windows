% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_build.R
\name{gdal_build_gdalraster}
\alias{gdal_build_gdalraster}
\title{Build a self-contained gdalraster package}
\usage{
gdal_build_gdalraster(
  gdal_home = default_gdal_home(),
  lib = NULL,
  user_lib = FALSE,
  source_tarball = NULL,
  repo = .gdalraster_repo,
  ref = "HEAD",
  upgrade = FALSE,
  repos = getOption("repos"),
  force = FALSE,
  enable_python = TRUE
)
}
\arguments{
\item{gdal_home}{Installed GDAL build runtime.}

\item{lib}{Destination library. The default is an isolated package-managed
library.}

\item{user_lib}{Install into \code{.libPaths()[1]}. This is destructive when an
existing \code{gdalraster} is installed there and requires \code{force = TRUE} in
non-interactive sessions.}

\item{source_tarball}{Optional local \code{gdalraster} source tarball.}

\item{repo}{Upstream source repository.}

\item{ref}{Git reference used for the source archive.}

\item{upgrade}{Install missing R dependencies before compiling.}

\item{repos}{CRAN-like repositories used when \code{upgrade = TRUE}.}

\item{force}{Replace an existing \code{gdalraster} package.}

\item{enable_python}{Provision the managed embedded-Python \code{.pth} file after
installation when possible.}
}
\value{
Invisibly, the destination library.
}
\description{
Compiles \code{gdalraster} against the managed GDAL SDK, vendors the bundle's
DLL dependency closure, GDAL/PROJ data, and pure-Python utilities into the
installed package, then records build provenance.
}
