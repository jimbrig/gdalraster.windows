% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_runtime.R
\name{install_gdalraster}
\alias{install_gdalraster}
\title{Install gdalraster from source against bundled GDAL}
\usage{
install_gdalraster(
  gdal_home = default_gdal_home(),
  lib = default_gdalraster_lib(),
  source_tarball = NULL,
  repo = "firelab/gdalraster",
  ref = "HEAD",
  upgrade = FALSE,
  repos = getOption("repos")
)
}
\arguments{
\item{gdal_home}{GDAL home directory used for compile/link flags.}

\item{lib}{Destination library path for installing gdalraster. Defaults to
an isolated package-managed library; pass \code{.libPaths()[1]} to install
into your default user library.}

\item{source_tarball}{Optional local path to \code{gdalraster_*.tar.gz}.}

\item{repo}{Source GitHub repo slug for gdalraster.}

\item{ref}{Git ref (branch, tag, commit) used when downloading from GitHub.}

\item{upgrade}{When \code{TRUE}, missing R package dependencies of gdalraster are
installed from \code{repos} before the source build. Has no effect on the
compile/link flags used to build gdalraster itself.}

\item{repos}{CRAN-like repositories used to satisfy R package dependencies
when \code{upgrade = TRUE}. Ignored when \code{upgrade = FALSE}.}
}
\value{
Invisibly returns installed library path.
}
\description{
Downloads or uses a local gdalraster source tarball and installs it from
source into a dedicated library path (default) so existing user libraries
are not overwritten. To install into your regular user library instead
(so plain \code{library(gdalraster)} resolves the source build without this
package's load helpers), pass it explicitly, e.g.
\code{install_gdalraster(lib = .libPaths()[1])}.
}
