% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_python.R
\name{gdal_enable_python}
\alias{gdal_enable_python}
\title{Enable GDAL embedded-Python utilities}
\usage{
gdal_enable_python(
  lib = NULL,
  isolated = FALSE,
  user_lib = NULL,
  python = NULL,
  site_packages = NULL,
  quiet = FALSE
)
}
\arguments{
\item{lib}{Library containing the self-contained \code{gdalraster} package.
Defaults to \code{.libPaths()[1]}.}

\item{isolated}{Use the package-managed isolated library instead of
\code{.libPaths()[1]}. Ignored when \code{lib} is set.}

\item{user_lib}{Deprecated. \code{user_lib = TRUE} is now the default;
\code{user_lib = FALSE} is equivalent to \code{isolated = TRUE}.}

\item{python}{Optional path to the CPython executable GDAL embeds.}

\item{site_packages}{Optional explicit \code{site-packages} directory.}

\item{quiet}{Suppress status messages.}
}
\value{
Invisibly, the \code{.pth} path, or \code{NULL} when no system CPython can be
located.
}
\description{
Writes a managed \code{.pth} file into the system CPython \code{site-packages}
directory. The file points to the pure-Python \code{osgeo_utils} vendored inside
the installed \code{gdalraster} package. This does not modify \code{PYTHONPATH} and
does not affect isolated virtual environments.
}
