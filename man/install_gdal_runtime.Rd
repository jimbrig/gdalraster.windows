% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_runtime.R
\name{install_gdal_runtime}
\alias{install_gdal_runtime}
\title{Install precompiled GDAL runtime}
\usage{
install_gdal_runtime(
  repo = .bundle_repo,
  tag = "latest",
  asset_pattern = .bundle_asset_pattern,
  gdal_home = default_gdal_home(),
  overwrite = FALSE,
  local_zip = NULL,
  fallback_zip = NULL
)
}
\arguments{
\item{repo}{GitHub repo slug publishing the runtime bundle releases.
Defaults to \code{"jimbrig/gdalraster.windows"}.}

\item{tag}{Release tag or \code{"latest"}. With \code{"latest"}, the newest release
that publishes a runtime bundle asset (matching \code{asset_pattern}) is
selected; releases without a bundle asset — such as R package releases
(\verb{v*}) — are skipped, so GitHub's "latest release" pointer does not need
to be a GDAL bundle release.}

\item{asset_pattern}{Regex used to select the release asset. Defaults to
the runtime bundle zip naming convention
(\verb{gdal-(bundle|ucrt64)-.*\\.zip$}).}

\item{gdal_home}{Destination GDAL home directory.}

\item{overwrite}{Whether to replace existing \code{gdal_home}.}

\item{local_zip}{Optional local GDAL runtime zip to install directly.}

\item{fallback_zip}{Optional fallback zip path used when release download
fails. When \code{NULL} (default), a vendored zip at
\code{inst/extdata/gdal-ucrt64-fallback.zip} is used if present; none ships
with the package, so by default no fallback is attempted.}
}
\value{
Invisibly returns installed GDAL home path.
}
\description{
Installs the GDAL runtime into \code{gdal_home} from one of:
}
\details{
\itemize{
\item \code{local_zip} (highest precedence),
\item GitHub release asset lookup/download,
\item \code{fallback_zip} when release lookup/download fails.
}

The selected zip must contain a GDAL root with \verb{bin/libgdal-*.dll}.
}
\section{Offline / air-gapped installation}{

On machines without network access, download the release asset manually
from \url{https://github.com/jimbrig/gdalraster.windows/releases}, transfer it
to the target machine, and install directly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{gdalraster.windows::install_gdal_runtime(
  local_zip = "C:/Downloads/gdal-ucrt64-v3.13.1-windows-x64.zip"
)
}\if{html}{\out{</div>}}

Note that no fallback zip is shipped with the package (a full runtime
bundle is too large to vendor), so the fallback path only applies when you
provide a \code{fallback_zip} yourself.
}

\section{Overwriting an installed runtime}{

With \code{overwrite = TRUE}, the existing \code{gdal_home} is deleted before the new
runtime is copied in. Runtime DLLs that were preloaded into the current
session (the package auto-bootstrap does this at load time when a runtime
is present) are unloaded first, but the session usually cannot fully
release its own locks anyway: runtime activation prepends \code{bin} to \code{PATH},
so DLLs loaded later in the session (the release download itself loads the
curl package, which maps the runtime's \code{zlib1.dll}) resolve dependencies
from the runtime directory outside R's control. Mapped DLLs cannot be
deleted but can be renamed, so such leftovers are moved into a stale
sibling directory
(\verb{<gdal_home>.stale-<pid>-<timestamp>}) and the install proceeds; stale
directories are deleted opportunistically by later installs once their
locks are gone. When this happens, restart R before building or loading
\code{gdalraster} so the new runtime — not the still-mapped old DLLs — is used.

Overwriting is refused outright while \code{gdalraster} is loaded (its DLL pins
the whole runtime in the process); reinstalling then requires a fresh R
session. If files are locked by \emph{other} processes (another R session, a
File Explorer preview pane), neither deletion nor the move-aside is
possible and the install aborts cleanly instead of leaving a
partially-replaced runtime.
}

