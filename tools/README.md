## tooling

Canonical CI scripts:

- `build_gdal.sh`: builds GDAL from source in MSYS2 UCRT64.
- `collect_dlls.sh`: assembles self-contained runtime bundle (`bin`, `include`, `lib`, `share`) and enforces dependency closure.
